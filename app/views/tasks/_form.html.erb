<div class='container'>
<%= simple_form_for(@task) do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <%= f.input :completed %>
    <%= f.submit 'Submit', class: 'btn btn-primary btn-block' %>
  <% end %>
  </div>
